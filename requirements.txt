langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
langchain-google-genai>=2.0.0   # Gemini on Vertex AI (express mode) — set LLM_PROVIDER=gemini
langchain-core>=0.3.0
openai>=1.40.0
anthropic>=0.34.0
streamlit>=1.38.0
pandas>=2.2.0
numpy>=1.26.0
pydantic>=2.8.0
python-dotenv>=1.0.0
langsmith>=0.1.100
requests>=2.31.0
plotly>=5.18.0

# Dense semantic retrieval (static embeddings, ~30 MB model, no torch).
# If this can't be installed/loaded, semantic.py auto-falls back to numpy TF-IDF.
model2vec>=0.3.0

# ── Optional upgrades (the system runs without these) ────────────────────────
# Faster/better fuzzy entity resolution (else stdlib difflib):  rapidfuzz>=3.0
# Richer learned-ranking models (else numpy logistic):          scikit-learn>=1.3